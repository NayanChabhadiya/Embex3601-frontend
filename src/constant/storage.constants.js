const STORAGE_KEYS = Object.freeze({
  ACCESS_TOKEN: "embex360_access_token",
  REFRESH_TOKEN: "embex360_refresh_token",
  AUTH_STATE: "embex360_auth_state",
});

const STORAGE_TYPES = Object.freeze({
  SESSION: "session",
  LOCAL: "local",
});

export { STORAGE_KEYS, STORAGE_TYPES };
